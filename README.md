# Recipe Finder — Vite + React + Tailwind

A small demo React app styled using TailwindCSS only. It runs on Vite.

What I changed

- Added Tailwind config (`tailwind.config.cjs`) and `postcss.config.cjs`.
- Replaced `src/index.css` with Tailwind directives and base styles.
- Replaced `src/App.jsx` with a Tailwind-styled Recipe Finder demo.
- Removed `src/App.css` and added Tailwind devDependencies to `package.json`.

Run locally

Open PowerShell in the project root and run:

```powershell
npm install
npm run dev
```

Then open the URL shown by Vite (usually http://localhost:5173).
